# MongoDB Settings
MONGODB_DBNAME=your_db_name_here # ex: Tamagochi
MONGODB_URI=mongodb+srv://${MONGODB_USER}:${MONGODB_PASSWORD}@${MONGODB_CLUSTER} # replace your password to code. ex: @ -> %40

# API Settings
PORT=8080
RATE_LIMITER_TIME=60000 # time window in milliseconds
RATE_LIMITER_MAX_REQUESTS=60 # max requests allowed in the time window. set to 0 for unlimited

# Cloudinary Settings 
CLOUDINARY_URL=cloudinary://<your_api_key>:<your_api_secret>@<cloud_name>
MAX_FILE_SIZE=5MB 

# Other Settings
AUTH_KEY=your_auth_key_here
CORS_ORIGIN=*
SERVER_URL=http://localhost:8080